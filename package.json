{
  "name": "gestion-de-tickets",
  "version": "0.0.0",
  "private": true,
  "description": "Sistema de Gestión de Tickets — monorepo (Symfony hexagonal + React). Prueba técnica IATSAE.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.4.2"
  },
  "lint-staged": {
    "apps/web/**/*.{ts,tsx,js,jsx,json,css,md,html,yml,yaml}": "prettier --write --ignore-unknown"
  }
}
